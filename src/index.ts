import {
    initialize,
    trackEvent,
    setUserId,
    setUserProperties,
    getUserId,
    getUserProperties,
    setSdkType,
    setSource,
} from './Client';

const notifly = {
    initialize,
    trackEvent,
    setUserProperties,
    setUserId,
    setSdkType,
    setSource,
    getUserId,
    getUserProperties,
};

export default notifly;
