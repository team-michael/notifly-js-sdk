import {
    initialize,
    trackEvent,
    setUserId,
    setUserProperties,
    getUserId,
    getUserProperties,
    deleteUser,
    setSdkType,
    setSource,
} from './Client';

const notifly = {
    initialize,
    trackEvent,
    setUserProperties,
    deleteUser,
    setUserId,
    setSdkType,
    setSource,
    getUserId,
    getUserProperties,
};

export default notifly;
