import {
    initialize,
    trackEvent,
    setUserId,
    setUserProperties,
    getUserId,
    getUserProperties,
    setSdkType,
    setSource,
    requestPermisson,
} from './Client';

const notifly = {
    initialize,
    trackEvent,
    setUserProperties,
    setUserId,
    setSdkType,
    setSource,
    getUserId,
    getUserProperties,
    requestPermisson,
};

export default notifly;
