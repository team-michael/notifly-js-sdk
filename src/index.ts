import {
    initialize,
    trackEvent,
    setUserId,
    setUserProperties,
    getUserId,
    getUserProperties,
    setSdkType,
    setSdkVersion,
    setSource,
    requestPermission,
} from './Client';

const notifly = {
    initialize,
    trackEvent,
    setUserProperties,
    setUserId,
    setSdkType,
    setSdkVersion,
    setSource,
    getUserId,
    getUserProperties,
    requestPermission,
};

export default notifly;
